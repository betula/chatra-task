{
  "name": "@charta-task/root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:server-api": "npm run dev --prefix server-api",
    "dev:client-web": "npm run dev --prefix client-web",
    "dev": "npx concurrently -k --names \"API,WEB\" -c \"white.bold,magenta.bold\" \"npm run dev:server-api\" \"npm run dev:client-web\"",
    "lerna:update": "npx lerna bootstrap",
    "lerna:reset": "npm run lerna:clean && npx lerna clean --yes && npm run lerna:update",
    "lerna:clean": "npx lerna run clean",
    "lerna:lint": "npx lerna run lint",
    "lerna:build": "npx lerna run build",
    "start": "npm run lerna:update && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "lerna": "^3.20.2"
  }
}
